## Packages
framer-motion | Complex animations for the dice roll and page transitions
lucide-react | Iconography for sports and UI elements
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes
canvas-confetti | For celebration effects when rolling a high score

## Notes
The application requires a specific winter olympics aesthetic.
Fonts: 'Outfit' for headings, 'DM Sans' for body text.
Authentication via Replit Auth is already set up, using `useAuth` hook.
Simulation page needs 3D-like dice animation.
